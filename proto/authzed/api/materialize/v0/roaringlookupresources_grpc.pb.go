// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             (unknown)
// source: authzed/api/materialize/v0/roaringlookupresources.proto

package v0

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	RoaringLookupResourcesService_ExperimentalRoaringLookupResources_FullMethodName = "/authzed.api.materialize.v0.RoaringLookupResourcesService/ExperimentalRoaringLookupResources"
)

// RoaringLookupResourcesServiceClient is the client API for RoaringLookupResourcesService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type RoaringLookupResourcesServiceClient interface {
	// EXPERIMENTAL: RoaringLookupResources returns a roaring64 bitmap of the IDs
	// of the resources of the given type on which the given subject has the
	// given permission. This API is experimental and subject to change or
	// removal.
	//
	// The bitmap is serialized in the RoaringFormatSpec 64-bit portable format
	// (https://github.com/RoaringBitmap/RoaringFormatSpec#extention-for-64-bit-implementations),
	// which OpenSearch consumes directly via a `"value_type": "bitmap"` terms
	// query against a `long` field, once Base64-encoded.
	//
	// The IDs in the bitmap are the resource object IDs exactly as they appear
	// in the relationships: no surrogate or internal ID is introduced, so the
	// caller can use the bitmap directly against its own data (for example, a
	// search index keyed by the same IDs).
	//
	// For this API to be usable, every resource object ID of the requested type
	// must be a canonical decimal integer that fits in 44 bits -- at most
	// 17592186044415 (2^44 - 1). Canonical means the string round-trips through
	// uint64 formatting unchanged: `document:007` and `document:7` are distinct
	// objects that would collide as the integer 7, so non-canonical IDs are
	// rejected. If any resource object ID violates either rule, the call fails
	// with FAILED_PRECONDITION rather than returning a partial bitmap, since a
	// bitmap that is quietly too small is a wrong authorization answer. When the
	// permission relates a type to itself (for example `group#member` looked up
	// for a `group#member` subject), the subject is one of its own resources, so
	// the subject's object ID is held to the same rules and can itself be the ID
	// named in that error.
	//
	// Response size: the whole bitmap is returned in a single unary message, and
	// most gRPC clients default to refusing messages larger than 4 MiB. Roaring
	// is compact -- a million sequential IDs encode in a few hundred bytes -- but
	// sparse IDs cost close to 10 bytes each, so a result of more than roughly
	// 400,000 widely-spread IDs can exceed that default and fail on the CLIENT
	// side with RESOURCE_EXHAUSTED ("received message larger than max"). This is
	// a limit of the caller's own gRPC configuration, not of the service: raise
	// it to match the largest result you expect (in Go,
	// grpc.WithDefaultCallOptions(grpc.MaxCallRecvMsgSize(n)); other languages
	// have an equivalent channel option). The `cardinality` field is returned so
	// callers can see how large a result is once received; no server-side result
	// limit is applied.
	ExperimentalRoaringLookupResources(ctx context.Context, in *ExperimentalRoaringLookupResourcesRequest, opts ...grpc.CallOption) (*ExperimentalRoaringLookupResourcesResponse, error)
}

type roaringLookupResourcesServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewRoaringLookupResourcesServiceClient(cc grpc.ClientConnInterface) RoaringLookupResourcesServiceClient {
	return &roaringLookupResourcesServiceClient{cc}
}

func (c *roaringLookupResourcesServiceClient) ExperimentalRoaringLookupResources(ctx context.Context, in *ExperimentalRoaringLookupResourcesRequest, opts ...grpc.CallOption) (*ExperimentalRoaringLookupResourcesResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ExperimentalRoaringLookupResourcesResponse)
	err := c.cc.Invoke(ctx, RoaringLookupResourcesService_ExperimentalRoaringLookupResources_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// RoaringLookupResourcesServiceServer is the server API for RoaringLookupResourcesService service.
// All implementations must embed UnimplementedRoaringLookupResourcesServiceServer
// for forward compatibility.
type RoaringLookupResourcesServiceServer interface {
	// EXPERIMENTAL: RoaringLookupResources returns a roaring64 bitmap of the IDs
	// of the resources of the given type on which the given subject has the
	// given permission. This API is experimental and subject to change or
	// removal.
	//
	// The bitmap is serialized in the RoaringFormatSpec 64-bit portable format
	// (https://github.com/RoaringBitmap/RoaringFormatSpec#extention-for-64-bit-implementations),
	// which OpenSearch consumes directly via a `"value_type": "bitmap"` terms
	// query against a `long` field, once Base64-encoded.
	//
	// The IDs in the bitmap are the resource object IDs exactly as they appear
	// in the relationships: no surrogate or internal ID is introduced, so the
	// caller can use the bitmap directly against its own data (for example, a
	// search index keyed by the same IDs).
	//
	// For this API to be usable, every resource object ID of the requested type
	// must be a canonical decimal integer that fits in 44 bits -- at most
	// 17592186044415 (2^44 - 1). Canonical means the string round-trips through
	// uint64 formatting unchanged: `document:007` and `document:7` are distinct
	// objects that would collide as the integer 7, so non-canonical IDs are
	// rejected. If any resource object ID violates either rule, the call fails
	// with FAILED_PRECONDITION rather than returning a partial bitmap, since a
	// bitmap that is quietly too small is a wrong authorization answer. When the
	// permission relates a type to itself (for example `group#member` looked up
	// for a `group#member` subject), the subject is one of its own resources, so
	// the subject's object ID is held to the same rules and can itself be the ID
	// named in that error.
	//
	// Response size: the whole bitmap is returned in a single unary message, and
	// most gRPC clients default to refusing messages larger than 4 MiB. Roaring
	// is compact -- a million sequential IDs encode in a few hundred bytes -- but
	// sparse IDs cost close to 10 bytes each, so a result of more than roughly
	// 400,000 widely-spread IDs can exceed that default and fail on the CLIENT
	// side with RESOURCE_EXHAUSTED ("received message larger than max"). This is
	// a limit of the caller's own gRPC configuration, not of the service: raise
	// it to match the largest result you expect (in Go,
	// grpc.WithDefaultCallOptions(grpc.MaxCallRecvMsgSize(n)); other languages
	// have an equivalent channel option). The `cardinality` field is returned so
	// callers can see how large a result is once received; no server-side result
	// limit is applied.
	ExperimentalRoaringLookupResources(context.Context, *ExperimentalRoaringLookupResourcesRequest) (*ExperimentalRoaringLookupResourcesResponse, error)
	mustEmbedUnimplementedRoaringLookupResourcesServiceServer()
}

// UnimplementedRoaringLookupResourcesServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedRoaringLookupResourcesServiceServer struct{}

func (UnimplementedRoaringLookupResourcesServiceServer) ExperimentalRoaringLookupResources(context.Context, *ExperimentalRoaringLookupResourcesRequest) (*ExperimentalRoaringLookupResourcesResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ExperimentalRoaringLookupResources not implemented")
}
func (UnimplementedRoaringLookupResourcesServiceServer) mustEmbedUnimplementedRoaringLookupResourcesServiceServer() {
}
func (UnimplementedRoaringLookupResourcesServiceServer) testEmbeddedByValue() {}

// UnsafeRoaringLookupResourcesServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to RoaringLookupResourcesServiceServer will
// result in compilation errors.
type UnsafeRoaringLookupResourcesServiceServer interface {
	mustEmbedUnimplementedRoaringLookupResourcesServiceServer()
}

func RegisterRoaringLookupResourcesServiceServer(s grpc.ServiceRegistrar, srv RoaringLookupResourcesServiceServer) {
	// If the following call panics, it indicates UnimplementedRoaringLookupResourcesServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&RoaringLookupResourcesService_ServiceDesc, srv)
}

func _RoaringLookupResourcesService_ExperimentalRoaringLookupResources_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ExperimentalRoaringLookupResourcesRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(RoaringLookupResourcesServiceServer).ExperimentalRoaringLookupResources(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: RoaringLookupResourcesService_ExperimentalRoaringLookupResources_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(RoaringLookupResourcesServiceServer).ExperimentalRoaringLookupResources(ctx, req.(*ExperimentalRoaringLookupResourcesRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// RoaringLookupResourcesService_ServiceDesc is the grpc.ServiceDesc for RoaringLookupResourcesService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var RoaringLookupResourcesService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "authzed.api.materialize.v0.RoaringLookupResourcesService",
	HandlerType: (*RoaringLookupResourcesServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "ExperimentalRoaringLookupResources",
			Handler:    _RoaringLookupResourcesService_ExperimentalRoaringLookupResources_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "authzed/api/materialize/v0/roaringlookupresources.proto",
}
